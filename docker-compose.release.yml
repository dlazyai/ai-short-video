x-common-volumes: &common-volumes
  - ./config.toml:/ai-short-video/config.toml
  - ./storage:/ai-short-video/storage

services:
  webui:
    image: ghcr.io/dlazyai/ai-short-video:latest
    container_name: "ai-short-video-webui"
    ports:
      - "127.0.0.1:8501:8501"
    command: [ "streamlit", "run", "./webui/Main.py", "--server.address=0.0.0.0", "--server.port=8501", "--browser.serverAddress=127.0.0.1", "--server.enableCORS=True", "--browser.gatherUsageStats=False", "--client.toolbarMode=minimal", "--logger.hideWelcomeMessage=True", "--server.showEmailPrompt=False" ]
    volumes: *common-volumes
    restart: always
  api:
    image: ghcr.io/dlazyai/ai-short-video:latest
    container_name: "ai-short-video-api"
    ports:
      - "127.0.0.1:8080:8080"
    command: [ "python3", "main.py" ]
    volumes: *common-volumes
    restart: always
